'use strict';

/* global it, describe, before, beforeEach, after, afterEach, slow */

const consts = {};

consts.APP_ID = 'portal-auth-test';
consts.REDIRECT_URI = 'http://localhost:3011';

module.exports = consts;
